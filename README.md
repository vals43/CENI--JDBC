TD : Push-down Processing - Système de Vote
🎯 Objectif
L'objectif principal de cet exercice est d'implémenter le Push-down processing : déléguer l'intelligence des calculs et des agrégations directement au moteur de base de données PostgreSQL.

Le but est d'éviter tout post-traitement en Java. Le code Java doit se contenter de récupérer des résultats déjà calculés par des requêtes SQL optimisées (utilisant COUNT, SUM, CASE WHEN, etc.).TD : Push-down Processing - Système de Vote
🎯 Objectif
L'objectif principal de cet exercice est d'implémenter le Push-down processing : déléguer l'intelligence des calculs et des agrégations directement au moteur de base de données PostgreSQL.

Le but est d'éviter tout post-traitement en Java. Le code Java doit se contenter de récupérer des résultats déjà calculés par des requêtes SQL optimisées (utilisant COUNT, SUM, CASE WHEN, etc.).
